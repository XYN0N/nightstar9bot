<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1>Welcome to Telegram Betting App</h1>
    <p>Join us and start betting with your friends!</p>
    <button class="btn btn-primary" onclick="joinTelegram()">Join</button>
    <div id="status" class="status"></div>
  </div>
  <script>
    function joinTelegram() {
      const telegramBotToken = '<%= telegramBotToken.split(":")[0] %>';
      const url = `https://telegram.me/${telegramBotToken}`;
      window.open(url, '_blank');
    }

    const urlParams = new URLSearchParams(window.location.search);
    if (urlParams.has('error')) {
      document.getElementById('status').innerText = 'Please send /start to @nightstar9bot first.';
    }
  </script>
</body>
</html>